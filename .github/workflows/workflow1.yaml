name: workflow 1
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -name: get code
       uses: actions/checkout@v3
      -name: Install dependencies
       run: npm ci
      -name: build code
       run: npm run build